import {createMuiTheme} from '@material-ui/core/styles';
import {indigo as color} from '@material-ui/core/colors';

// Create a theme instance.
const theme = createMuiTheme({

    palette: {

        primary: color

    },

});

export default theme;
